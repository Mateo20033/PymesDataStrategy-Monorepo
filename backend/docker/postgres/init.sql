-- PostgreSQL init script for PymesDataStrategy (ETL DB)
-- Prisma handles schema creation via migrations at API startup.

CREATE EXTENSION IF NOT EXISTS "pgcrypto";
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
